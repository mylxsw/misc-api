[project]
name = "misc-api"
version = "0.1.0"
description = "A collection of various apis"
requires-python = ">=3.11"
dependencies = [
    "flask",
    "gunicorn",
    "dashscope",
    "pillow",
    "requests",
    "websockets>=15.0.1",
]

[tool.uv]
dev-dependencies = []
